- if params[:controller] == 'lessons'
  %b Lessons:
  - @lessons.all.each do |l|
    = link_to l.number, '#'
- elsif params[:controller] == 'translate_me_game'
  %b Games:
  - @lessons.all.each do |l|
    = link_to l.number, translate_me_category_path(@category, l.number)